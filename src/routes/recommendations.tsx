import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, Check, MapPin, Sparkles } from "lucide-react";
import { venues } from "@/lib/venues";
import { Button } from "@/components/ui/button";
export const Route=createFileRoute("/recommendations")({head:()=>({meta:[{title:"Your accessible place matches — FriendEase"},{name:"description",content:"Ranked cafe recommendations matched privately to Sammy’s profile."},{property:"og:title",content:"Your accessible place matches — FriendEase"},{property:"og:description",content:"Three accessibility-aware cafe recommendations for your group."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Recommendations});
function Recommendations(){return <div className="mx-auto max-w-6xl px-5 py-12 lg:px-8"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-sm font-bold text-primary">3 places found for your group</p><h1 className="mt-2 text-4xl font-extrabold">Best matches near Richmond</h1><p className="mt-3 text-muted-foreground">Ranked against Sammy’s required and important preferences.</p></div><span className="rounded-md bg-success-soft px-4 py-2 text-sm font-bold text-success">All required needs met</span></div><div className="mt-9 grid gap-6">{venues.map((v,index)=><article key={v.id} className={`grid overflow-hidden rounded-lg border bg-background shadow-sm md:grid-cols-[0.8fr_1.2fr] ${index===0?"ring-2 ring-primary":""}`}><div className="relative"><img src={v.image} loading="lazy" width={1280} height={768} alt={`${v.name} interior`} className="h-full min-h-64 w-full object-cover"/>{index===0&&<span className="absolute left-4 top-4 rounded-md bg-primary px-3 py-1.5 text-xs font-extrabold text-primary-foreground">TOP MATCH</span>}</div><div className="p-6"><div className="grid grid-cols-[minmax(0,1fr)_auto] gap-4"><div className="min-w-0"><h2 className="truncate text-2xl font-bold">{v.name}</h2><p className="mt-1 flex items-center gap-1 text-sm text-muted-foreground"><MapPin className="size-4"/>{v.location}</p></div><div className="text-right"><div className="text-3xl font-extrabold text-primary">{v.match}%</div><div className="text-xs font-bold text-muted-foreground">MATCH</div></div></div><div className="mt-5 flex flex-wrap gap-2">{v.features.map(f=><span key={f} className="inline-flex items-center gap-1 rounded-md bg-secondary px-2.5 py-1.5 text-xs font-semibold"><Check className="size-3 text-success"/>{f}</span>)}</div><div className="mt-5 rounded-md bg-muted p-4"><p className="flex items-center gap-2 text-sm font-bold"><Sparkles className="size-4 text-primary"/>Why this matches you</p><p className="mt-2 text-sm leading-6 text-muted-foreground">{v.why}</p></div><Button asChild variant={index===0?"default":"outline"} className="mt-5"><Link to="/venues/$venueId" params={{venueId:v.id}}>View details <ArrowRight/></Link></Button></div></article>)}</div></div>}